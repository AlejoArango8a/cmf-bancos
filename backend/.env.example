# Copia este archivo a .env en tu PC (no subas .env a Git).
# En Render: Environment → Add Environment Variable (mismos nombres).

SUPABASE_URL=https://xxxx.supabase.co
SUPABASE_ANON_KEY=eyJ...

# Orígenes permitidos para CORS (separados por coma). * = cualquiera (solo pruebas).
FRONTEND_URLS=*
